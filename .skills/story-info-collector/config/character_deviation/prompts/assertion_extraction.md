阶段：断言提取
角色：{{character}}
场景：{{scenario}}
输出格式：JSON

断言类型配置：
{{assertion_types}}

资料片段：
{{source_snippets}}

请抽取角色相关断言。每条断言必须包含 assertion_type、subject、claim、confidence、source_id、source_type、source_channel、quote、version。不要合并不同来源的断言。
